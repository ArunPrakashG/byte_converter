name: byte_converter
description: Provides a simple interface for conversion of Digital values such as Bytes, KiloBytes etc. Now with BigInt support for arbitrary precision calculations.
version: 2.2.0
homepage: https://github.com/ArunPrakashG/byte_converter

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.14.4
  personal_flutter_lints:
    git:
      url: https://github.com/ArunPrakashG/personal_flutter_lints.git
      ref: main
