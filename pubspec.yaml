name: byte_converter
description: High-performance byte and data-rate conversions with BigInt precision, expression parsing, transfer planning, storage profiles, and localization-ready formatting.
version: 2.5.0
homepage: https://github.com/ArunPrakashG/byte_converter
repository: https://github.com/ArunPrakashG/byte_converter
issue_tracker: https://github.com/ArunPrakashG/byte_converter/issues
documentation: https://github.com/ArunPrakashG/byte_converter/wiki
topics:
  - bytes
  - data-conversion
  - unit-conversion
  - formatting
  - bigint

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  intl: ^0.19.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
  filesize: ^2.0.1

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

executables:
  bytec: bytec
